#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
  int n;

    scanf("%d", &n);  // Read number of elements

    int arr[n];

    // Read array elements

    for (int i = 0; i < n; i++) {

        scanf("%d", &arr[i]);

    }

    // Print array in reverse order

    for (int i = n - 1; i >= 0; i--) {

        printf("%d", arr[i]);

        if (i != 0)

            printf(" "); // Add space between numbers

    }

    return 0;

}   
    
